/*Podemos ver que o let é uma "var", so que isolada, ou seja, fora do bloco function o let a='carro' nao existe mais, a nao ser que seja uma var.*/
function ver(){
  let a='carro'
}
console.log(a)
